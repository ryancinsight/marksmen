--- examples/roundtrips.rs
+++ examples/roundtrips.rs
@@ -53,7 +53,7 @@
     
     let input_dir = std::path::Path::new(".");
     
-    let docx_bytes = marksmen_docx::translation::document::convert(events_docx, &config, input_dir)?;
+    let docx_bytes = marksmen_docx::translation::document::convert(events_docx, &config, input_dir, None)?;
     std::fs::write(&report_docx_path, &docx_bytes)?;
     println!("Written roundtrip-generated structure back to: {}", report_docx_path.display());
 
--- examples/conversions.rs
+++ examples/conversions.rs
@@ -23,7 +23,7 @@
     let events_docx = marksmen_core::parsing::parser::parse(&markdown);
     let config = Config::default();
     
-    let docx_bytes = marksmen_docx::translation::document::convert(events_docx, &config, &root)?;
+    let docx_bytes = marksmen_docx::translation::document::convert(events_docx, &config, &root, None)?;
     std::fs::write("example.docx", &docx_bytes)?;
     println!("Written DOCX generation back to: example.docx");
 
